# CSS Gradient Generator

A visual CSS gradient generator: pick linear or radial, set the angle and two colours, preview live, and copy the CSS. Zero dependencies — works in plain HTML, React, Vue, Svelte or Astro.

**▶ [Live demo](https://sgbp.tech/tools/css-gradient-generator)**

```html
<script src="gradient-generator.js"></script>
<gradient-generator></gradient-generator>
```

## What it does

A CSS gradient is a smooth blend between colours, drawn by the browser with no image file. `linear-gradient()` runs along a line at a chosen angle; `radial-gradient()` spreads outward from a centre point. Because they are pure CSS, they stay sharp at any size and add zero network requests.

## Install

```bash
npm install @sgbp/gradient-generator
```

or copy `gradient-generator.js` into your project.

## Further reading

Maintained by [SGBP — Singapore Build Partners](https://sgbp.tech), a studio building fast,
accessible websites for Singapore SMEs, as one of a set of free developer tools.

## License

MIT © SGBP. Contributions welcome.
